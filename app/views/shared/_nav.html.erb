
<ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <%= content_tag :li, link_to('Dashboard', dashboard_index_path) %>
    <%= content_tag :li, link_to('Profile', user_path(current_user)) %>
    <%= content_tag :li, link_to('Settings', edit_user_path(current_user)) %>
    <%= content_tag :li, link_to('Admin', admin_users_path) if current_user.admin? %>
  <% end %>
</ul>
